ALGORITHM insert
VAR
    tab : ARRAY_OF INTEGER []
    key,i,j:INTEGER
BEGIN
    FOR i FROM 1 TO 10  DO
        READ(key)
        inserted:=false
        j:=tab.length
        WHILE(j>0 AND inserted=false)
            IF (tab[j-1]>key ) THEN
                tab[j+1]=tab[j]
                j:=j-1
            ELSE
                inserted:=true
            END_IF
        END_WHILE
    tab[j]=key
    END_FOR
END